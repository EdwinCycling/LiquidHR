'use client'

import { ArrowDown, ArrowUp, Check, Plus, Save, Search, X } from 'lucide-react'
import { useMemo, useState, type FormEvent } from 'react'
import { useRouter } from 'next/navigation'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { DropdownSelect } from '@/components/ui/dropdown-select'
import { Surface } from '@/components/ui/surface'
import { TextInput } from '@/components/ui/text-input'
import { FormField } from '@/components/patterns/form-field'
import { IconButton } from '@/components/ui/icon-button'
import type { EnpsBankQuestionRow, EnpsCategoryRow } from '@/lib/research/database'
import type { EnpsDraftData } from '@/lib/research/admin-service'
import type { ResearchTargetOptions } from '@/lib/research/target-service'
import type { EnpsQuestionType, ResearchTargetMode } from '@/lib/research/schemas'
import { ResearchTargetPicker } from './research-target-picker'

interface EnpsLabels { campaign: string; title: string; startsAt: string; endsAt: string; target: string; targetMode: string; targetAll: string; targetDepartments: string; targetLocations: string; targetEntities: string; targetEmployees: string; targetSearch: string; targetEmpty: string; selected: string; questions: string; scale: string; scale10: string; likert5: string; likert4: string; openText: string; yesNo: string; reminderInterval: string; mandatoryEnps: string; mandatoryDescription: string; questionBank: string; questionBankSearch: string; categoryAll: string; addBankQuestion: string; added: string; selectedQuestions: string; remove: string; moveUp: string; moveDown: string; enabled: string; disabled: string; customQuestion: string; customQuestionPlaceholder: string; addCustomQuestion: string; addingCustomQuestion: string; saveDraft: string; saving: string; saveFailed: string }
function toDateTimeLocal(value: string): string { const date = new Date(value); const pad = (part: number) => String(part).padStart(2, '0'); return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}T${pad(date.getHours())}:${pad(date.getMinutes())}` }

export function EnpsBuilder({ labels, targets, categories, bank, draft }: { labels: EnpsLabels; targets: ResearchTargetOptions; categories: EnpsCategoryRow[]; bank: EnpsBankQuestionRow[]; draft?: EnpsDraftData }) {
  const router = useRouter()
  const mandatory = bank.find((question) => question.is_mandatory_enps)
  const [availableQuestions, setAvailableQuestions] = useState(bank)
  const [title, setTitle] = useState(draft?.title ?? '')
  const [startsAt, setStartsAt] = useState(draft ? toDateTimeLocal(draft.startsAt) : '')
  const [endsAt, setEndsAt] = useState(draft ? toDateTimeLocal(draft.endsAt) : '')
  const [scale, setScale] = useState<'LIKERT_5' | 'LIKERT_4' | 'SCALE_10'>(draft?.scaleType ?? 'LIKERT_5')
  const [reminderInterval, setReminderInterval] = useState(draft?.reminderIntervalDays ?? 7)
  const [mode, setMode] = useState<ResearchTargetMode>(draft?.targetMode ?? 'ALL')
  const [selectedIds, setSelectedIds] = useState<string[]>(draft?.targetIds ?? [])
  const [selectedQuestions, setSelectedQuestions] = useState<string[]>(draft ? [...draft.questions].sort((left, right) => left.order - right.order).map((question) => question.bankQuestionId) : (mandatory ? [mandatory.id] : []))
  const [disabledQuestions, setDisabledQuestions] = useState<string[]>(draft?.questions.filter((question) => !question.enabled).map((question) => question.bankQuestionId) ?? [])
  const [customQuestion, setCustomQuestion] = useState('')
  const [customCategoryId, setCustomCategoryId] = useState(categories[0]?.id ?? '')
  const [customType, setCustomType] = useState<EnpsQuestionType>('LIKERT_5')
  const [addingCustom, setAddingCustom] = useState(false)
  const [search, setSearch] = useState('')
  const [categoryId, setCategoryId] = useState('ALL')
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const categoryById = useMemo(() => new Map(categories.map((category) => [category.id, category.name])), [categories])
  const filteredBank = useMemo(() => { const query = search.trim().toLocaleLowerCase(); return availableQuestions.filter((question) => !question.is_mandatory_enps && (categoryId === 'ALL' || question.category_id === categoryId) && (!query || question.question_text.toLocaleLowerCase().includes(query))) }, [availableQuestions, categoryId, search])
  const selectedRows = selectedQuestions.flatMap((id) => { const question = availableQuestions.find((item) => item.id === id); return question ? [question] : [] })
  function moveQuestion(index: number, direction: -1 | 1): void { setSelectedQuestions((current) => { const target = index + direction; if (index === 0 || target < 1 || target >= current.length) return current; const next = [...current]; [next[index], next[target]] = [next[target]!, next[index]!]; return next }) }
  function removeQuestion(id: string): void { setSelectedQuestions((current) => current.filter((questionId) => questionId !== id)); setDisabledQuestions((current) => current.filter((questionId) => questionId !== id)) }
  async function addCustomQuestion(): Promise<void> {
    if (!customQuestion.trim() || !customCategoryId) return
    setAddingCustom(true); setError(null)
    try { const response = await fetch('/api/research/question-bank/questions', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ categoryId: customCategoryId, text: customQuestion, type: customType }) }); if (!response.ok) throw new Error('custom-question'); const result = await response.json() as { id: string }; const question: EnpsBankQuestionRow = { id: result.id, tenant_id: '', hr_group_id: '', category_id: customCategoryId, question_number: null, question_text: customQuestion.trim(), default_type: customType, is_mandatory_enps: false, is_system: false }; setAvailableQuestions((current) => [...current, question]); setSelectedQuestions((current) => [...current, question.id]); setCustomQuestion('') } catch { setError(labels.saveFailed) } finally { setAddingCustom(false) }
  }
  async function save(event: FormEvent<HTMLFormElement>): Promise<void> {
    event.preventDefault(); setError(null); setSaving(true)
    try { const questions = selectedRows.map((question, index) => ({ bankQuestionId: question.id, order: index + 1, type: (question.is_mandatory_enps ? 'SCALE_10' : question.default_type === 'OPEN_TEXT' || question.default_type === 'YES_NO' ? question.default_type : scale) as EnpsQuestionType, mandatory: question.is_mandatory_enps, enabled: !disabledQuestions.includes(question.id) })); const response = await fetch(draft ? `/api/research/enps/${draft.id}` : '/api/research/enps', { method: draft ? 'PUT' : 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title, startsAt: new Date(startsAt).toISOString(), endsAt: new Date(endsAt).toISOString(), scaleType: scale, reminderIntervalDays: reminderInterval, target: { mode, ids: mode === 'ALL' ? [] : selectedIds }, questions }) }); if (!response.ok) throw new Error('save'); router.push('/settings/research'); router.refresh() } catch { setError(labels.saveFailed) } finally { setSaving(false) }
  }

  return <form className="space-y-6" onSubmit={(event) => void save(event)}><Surface className="p-5 sm:p-7"><h2 className="text-xl font-semibold">{labels.campaign}</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><FormField className="sm:col-span-2" control={<TextInput maxLength={255} minLength={3} onChange={(event) => setTitle(event.target.value)} required value={title} />} label={labels.title} required /><FormField control={<TextInput onChange={(event) => setStartsAt(event.target.value)} required type="datetime-local" value={startsAt} />} label={labels.startsAt} required /><FormField control={<TextInput min={startsAt} onChange={(event) => setEndsAt(event.target.value)} required type="datetime-local" value={endsAt} />} label={labels.endsAt} required /><FormField control={<DropdownSelect onChange={(event) => setScale(event.target.value as typeof scale)} searchable value={scale}><option value="LIKERT_5">{labels.likert5}</option><option value="LIKERT_4">{labels.likert4}</option><option value="SCALE_10">{labels.scale10}</option></DropdownSelect>} label={labels.scale} required /><FormField control={<TextInput max={30} min={1} onChange={(event) => setReminderInterval(Number(event.target.value))} required type="number" value={reminderInterval} />} label={labels.reminderInterval} required /></div></Surface><Surface className="p-5 sm:p-7"><h2 className="text-xl font-semibold">{labels.target}</h2><div className="mt-5"><ResearchTargetPicker labels={{ targetMode: labels.targetMode, targetAll: labels.targetAll, targetDepartments: labels.targetDepartments, targetLocations: labels.targetLocations, targetEntities: labels.targetEntities, targetEmployees: labels.targetEmployees, targetSearch: labels.targetSearch, targetEmpty: labels.targetEmpty, selected: labels.selected }} mode={mode} onModeChange={setMode} onSelectedIdsChange={setSelectedIds} options={targets} selectedIds={selectedIds} /></div></Surface><Surface className="p-5 sm:p-7"><h2 className="text-xl font-semibold">{labels.questions}</h2>{mandatory ? <Surface className="mt-5 flex items-start gap-4 p-4" variant="subtle"><Badge tone="info"><Check aria-hidden="true" size={17} /></Badge><div><p className="text-sm font-semibold">{labels.mandatoryEnps}</p><p className="mt-1 text-sm leading-6 text-muted-foreground">{mandatory.question_text || labels.mandatoryDescription}</p></div></Surface> : null}<Surface className="mt-5 p-4" variant="subtle"><h3 className="font-semibold">{labels.customQuestion}</h3><div className="mt-3 grid gap-3 lg:grid-cols-[minmax(0,1fr)_13rem_11rem_auto]"><FormField className="lg:col-span-1" control={<TextInput maxLength={2000} minLength={2} onChange={(event) => setCustomQuestion(event.target.value)} placeholder={labels.customQuestionPlaceholder} value={customQuestion} />} label={labels.customQuestion} /><FormField control={<DropdownSelect onChange={(event) => setCustomCategoryId(event.target.value)} searchable value={customCategoryId}>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</DropdownSelect>} label={labels.categoryAll} /><FormField control={<DropdownSelect onChange={(event) => setCustomType(event.target.value as EnpsQuestionType)} searchable value={customType}><option value="LIKERT_5">{labels.likert5}</option><option value="LIKERT_4">{labels.likert4}</option><option value="SCALE_10">{labels.scale10}</option><option value="OPEN_TEXT">{labels.openText}</option><option value="YES_NO">{labels.yesNo}</option></DropdownSelect>} label={labels.scale} /><Button className="self-end" disabled={addingCustom || !customQuestion.trim() || !customCategoryId} loading={addingCustom} onClick={() => void addCustomQuestion()} type="button" variant="secondary"><Plus aria-hidden="true" size={15} />{labels.addCustomQuestion}</Button></div></Surface><div className="mt-6 grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><div><h3 className="font-semibold">{labels.questionBank}</h3><div className="mt-3 grid gap-3 sm:grid-cols-[1fr_14rem]"><TextInput aria-label={labels.questionBankSearch} leadingIcon={<Search aria-hidden="true" />} onChange={(event) => setSearch(event.target.value)} placeholder={labels.questionBankSearch} value={search} /><DropdownSelect aria-label={labels.categoryAll} onChange={(event) => setCategoryId(event.target.value)} searchable value={categoryId}><option value="ALL">{labels.categoryAll}</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</DropdownSelect></div><div className="mt-3 max-h-[32rem] space-y-2 overflow-y-auto pr-1">{filteredBank.map((question) => { const selected = selectedQuestions.includes(question.id); return <Surface className="flex items-start justify-between gap-3 p-4" key={question.id} variant="subtle"><div><p className="text-xs font-semibold text-primary">{categoryById.get(question.category_id)}</p><p className="mt-1 text-sm leading-6">{question.question_text}</p></div><Button disabled={selected} onClick={() => setSelectedQuestions((current) => [...current, question.id])} size="sm" type="button" variant={selected ? 'secondary' : 'primary'}>{selected ? <Check aria-hidden="true" size={15} /> : <Plus aria-hidden="true" size={15} />}{selected ? labels.added : labels.addBankQuestion}</Button></Surface> })}</div></div><aside><h3 className="font-semibold">{labels.selectedQuestions}</h3><div className="mt-3 space-y-2">{selectedRows.map((question, index) => <Surface className="p-4" key={question.id} variant="subtle"><div className="flex items-start gap-3"><Badge tone="info">{index + 1}</Badge><p className="flex-1 text-sm leading-6">{question.question_text}</p>{!question.is_mandatory_enps ? <IconButton label={labels.remove} onClick={() => removeQuestion(question.id)} size="sm" type="button" variant="danger"><X aria-hidden="true" /></IconButton> : null}</div></Surface>)}</div></aside></div></Surface><Surface className="p-4"><p className="text-sm font-semibold">{labels.selectedQuestions}</p><div className="mt-3 space-y-2">{selectedRows.map((question, index) => { const disabled = disabledQuestions.includes(question.id); return <div className="flex items-center gap-3 border-b border-border-subtle py-2 last:border-0" key={question.id}><span className="min-w-0 flex-1 truncate text-sm">{index + 1}. {question.question_text}</span>{!question.is_mandatory_enps ? <><Button aria-pressed={!disabled} onClick={() => setDisabledQuestions((current) => disabled ? current.filter((id) => id !== question.id) : [...current, question.id])} size="sm" type="button" variant={disabled ? 'secondary' : 'primary'}>{disabled ? labels.disabled : labels.enabled}</Button><IconButton disabled={index <= 1} label={labels.moveUp} onClick={() => moveQuestion(index, -1)} size="sm" type="button" variant="secondary"><ArrowUp aria-hidden="true" /></IconButton><IconButton disabled={index === selectedRows.length - 1} label={labels.moveDown} onClick={() => moveQuestion(index, 1)} size="sm" type="button" variant="secondary"><ArrowDown aria-hidden="true" /></IconButton></> : null}</div> })}</div></Surface>{error ? <p className="border border-destructive/30 bg-destructive-surface p-4 text-sm font-medium text-destructive" role="alert">{error}</p> : null}<div className="flex justify-end"><Button disabled={saving || !mandatory} loading={saving} type="submit"><Save aria-hidden="true" size={16} />{saving ? labels.saving : labels.saveDraft}</Button></div></form>
}
