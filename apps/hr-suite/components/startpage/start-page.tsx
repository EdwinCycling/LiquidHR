/* eslint-disable @next/next/no-img-element -- administration branding is served by an authenticated route. */
import Link from 'next/link'
import { ArrowRight, BriefcaseBusiness, Building2, CalendarDays, CheckCircle2, CircleDashed, FileText, HeartPulse, LayoutDashboard, Sparkles, Users, Wrench } from 'lucide-react'
import { formatDateTime } from '@/lib/preferences/formatters'
import type { DateFormat, TimeFormat } from '@/lib/preferences/user-preferences'
import type { Locale } from '@/lib/i18n/config'
import type { StartPageData } from '@/lib/startpage/service'

interface StartPageLabels {
  eyebrow: string; headline: string; subtitle: string; activeScope: string; administration: string; tenant: string; peopleInScope: string; operationalTitle: string; operationalBody: string; liveSource: string; employeesTitle: string; employeesDescription: string; organizationTitle: string; organizationDescription: string; absenceTitle: string; absenceDescription: string; documentsTitle: string; documentsDescription: string; openEmployees: string; openOrganization: string; openAbsence: string; openDocuments: string; notAvailable: string; yourPriorities: string; prioritiesBody: string; remindersTitle: string; remindersDescription: string; openReminders: string; noReminders: string; moreReminders: string; workInProgress: string; workInProgressBody: string; futureDeclarations: string; futureContracts: string; futureAssets: string; futureTasks: string; futureEvents: string; futureSource: string; dashboardHint: string; openDashboard: string; quickLinks: string; calendar: string; insights: string; updated: string; fallbackName: string; absenceCasesTitle: string; absenceCasesDescription: string; absenceSince: string; absenceDays: string; openAbsenceDossier: string; noActiveAbsences: string; absenceRecovery: string
}

interface StartPageProps { data: StartPageData; locale: Locale; dateFormat: DateFormat; timeFormat: TimeFormat; logoUrl?: string | null; greeting: string; labels: StartPageLabels }

function MetricCard({ value, icon: Icon, title, description, href, action, labels, tone = 'blue' }: { value: number | null; icon: typeof Users; title: string; description: string; href: string; action: string; labels: Pick<StartPageLabels, 'liveSource' | 'notAvailable'>; tone?: 'blue' | 'mint' | 'amber' | 'rose' }) {
  const toneClasses = { blue: 'bg-accent text-accent-foreground', mint: 'bg-success-surface text-success', amber: 'bg-warning-surface text-warning', rose: 'bg-destructive-surface text-destructive' } as const
  return <article className="group flex min-h-52 flex-col justify-between rounded-[1.35rem] border bg-surface p-5 shadow-[0_1rem_2.5rem_color-mix(in_srgb,var(--primary)_7%,transparent)] transition duration-200 hover:-translate-y-1 hover:shadow-[0_1.5rem_3rem_color-mix(in_srgb,var(--primary)_13%,transparent)] sm:p-6">
    <div className="flex items-start justify-between gap-4"><span className={`grid size-11 place-items-center rounded-2xl ${toneClasses[tone]}`}><Icon aria-hidden="true" size={20} /></span><span className="inline-flex items-center gap-1.5 text-[0.68rem] font-bold uppercase tracking-[0.14em] text-muted-foreground"><span className="size-1.5 rounded-full bg-success" />{labels.liveSource}</span></div>
    <div className="mt-7"><p className="text-sm font-semibold text-foreground">{title}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{description}</p><p className="mt-4 text-4xl font-semibold tracking-[-0.06em] text-foreground">{value ?? '—'}</p></div>
    <Link className="mt-6 inline-flex w-fit items-center gap-2 text-sm font-semibold text-accent-foreground underline-offset-4 hover:underline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-focus" href={href}>{value === null ? labels.notAvailable : action}<ArrowRight aria-hidden="true" size={15} /></Link>
  </article>
}

export function StartPage({ data, locale, dateFormat, timeFormat, logoUrl, greeting, labels }: StartPageProps) {
  const name = data.firstName ?? labels.fallbackName
  const scopeName = data.administrationName ?? data.tenantName
  const dateLocale = locale === 'nl' ? 'nl-NL' : 'en-GB'
  const futureItems = [labels.futureDeclarations, labels.futureContracts, labels.futureAssets, labels.futureTasks, labels.futureEvents]
  return <div className="relative min-h-full overflow-hidden bg-background"><div aria-hidden="true" className="pointer-events-none absolute -right-36 -top-36 size-[28rem] rounded-full bg-accent/55 blur-3xl" /><div aria-hidden="true" className="pointer-events-none absolute -bottom-48 left-1/4 size-[24rem] rounded-full bg-success-surface/60 blur-3xl" />
    <main className="relative mx-auto w-full max-w-[90rem] px-4 py-6 sm:px-8 sm:py-9 lg:px-10">
      {logoUrl ? <div className="mb-4 flex justify-end"><img alt="" className="max-h-12 max-w-48 object-contain" src={logoUrl} /></div> : null}
      <section className="relative overflow-hidden rounded-[2rem] border border-primary/15 bg-primary p-6 text-primary-foreground shadow-[0_1.6rem_4rem_color-mix(in_srgb,var(--primary)_24%,transparent)] sm:p-8 lg:p-10"><div aria-hidden="true" className="absolute -right-16 -top-24 size-72 rounded-full border border-primary-foreground/10 bg-primary-foreground/5 blur-2xl" /><div aria-hidden="true" className="absolute -bottom-28 right-1/3 size-56 rounded-full bg-primary-foreground/10 blur-3xl" /><div className="relative grid gap-9 lg:grid-cols-[minmax(0,1fr)_19rem] lg:items-end"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.2em] text-primary-foreground/65"><Sparkles aria-hidden="true" size={15} />{labels.eyebrow}</div><h1 className="mt-5 max-w-3xl text-3xl font-semibold tracking-[-0.05em] sm:text-5xl lg:text-[3.6rem]">{greeting}, {name} <span aria-hidden="true">👋</span></h1><p className="mt-4 max-w-2xl text-base leading-7 text-primary-foreground/75 sm:text-lg">{labels.headline} {labels.subtitle}</p><div className="mt-7 flex flex-wrap gap-3"><span className="inline-flex items-center gap-2 rounded-full border border-primary-foreground/15 bg-primary-foreground/10 px-3.5 py-2 text-sm text-primary-foreground/85"><Building2 aria-hidden="true" size={15} />{scopeName}</span><span className="inline-flex items-center gap-2 rounded-full border border-primary-foreground/15 bg-primary-foreground/10 px-3.5 py-2 text-sm text-primary-foreground/85"><CheckCircle2 aria-hidden="true" size={15} />{labels.activeScope}</span></div></div><div className="rounded-[1.5rem] border border-primary-foreground/15 bg-primary-foreground/10 p-5 backdrop-blur-sm"><p className="text-xs font-bold uppercase tracking-[0.16em] text-primary-foreground/60">{labels.activeScope}</p><p className="mt-3 truncate text-lg font-semibold">{scopeName}</p><p className="mt-1 text-sm text-primary-foreground/65">{data.administrationName ? labels.administration : labels.tenant}</p><div className="mt-6 border-t border-primary-foreground/15 pt-5"><p className="text-4xl font-semibold tracking-[-0.06em]">{data.employees ?? '—'}</p><p className="mt-1 text-sm text-primary-foreground/65">{labels.peopleInScope}</p></div></div></div></section>

      <div className="mt-10 grid gap-8 lg:grid-cols-[minmax(0,1.38fr)_minmax(19rem,.62fr)]"><section><header className="mb-5 flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">{labels.operationalTitle}</p><h2 className="mt-1 text-2xl font-semibold tracking-tight">{labels.operationalTitle}</h2><p className="mt-1 text-sm text-muted-foreground">{labels.operationalBody}</p></div><span className="text-sm text-muted-foreground">{new Intl.DateTimeFormat(dateLocale, { dateStyle: 'full' }).format(new Date())}</span></header><div className="grid gap-4 sm:grid-cols-2"><MetricCard value={data.employees} icon={Users} title={labels.employeesTitle} description={labels.employeesDescription} href="/employees" action={labels.openEmployees} labels={labels} tone="blue" /><MetricCard value={data.departments} icon={Building2} title={labels.organizationTitle} description={labels.organizationDescription} href="/organization-chart" action={labels.openOrganization} labels={labels} tone="mint" /><MetricCard value={data.activeAbsences} icon={HeartPulse} title={labels.absenceTitle} description={labels.absenceDescription} href="/insights?report=absence" action={labels.openAbsence} labels={labels} tone="rose" /><MetricCard value={data.companyDocuments} icon={FileText} title={labels.documentsTitle} description={labels.documentsDescription} href="/company-documents" action={labels.openDocuments} labels={labels} tone="amber" /></div><section className="mt-6 overflow-hidden rounded-[1.5rem] border bg-surface shadow-sm"><header className="border-b px-5 py-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-muted-foreground">{labels.absenceCasesTitle}</p><h3 className="mt-1 font-semibold">{labels.absenceCasesDescription}</h3></div><Link className="inline-flex items-center gap-1 text-sm font-semibold text-accent-foreground hover:underline" href="/insights?report=absence">{labels.openAbsence}<ArrowRight aria-hidden="true" size={15} /></Link></div></header>{data.activeAbsenceItems.length ? <ul className="divide-y divide-border/70">{data.activeAbsenceItems.map((item) => <li key={item.caseId}><Link className="flex flex-col gap-2 px-5 py-4 transition hover:bg-muted/35 sm:flex-row sm:items-center sm:justify-between" href={`/employees/${item.employeeId}?tab=absence`}><span className="flex min-w-0 items-center gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-full bg-destructive-surface text-destructive"><HeartPulse aria-hidden="true" size={17} /></span><span className="min-w-0"><span className="block truncate font-semibold text-foreground">{item.employeeName}</span><span className="mt-1 block text-sm text-muted-foreground">{labels.absenceSince} {new Intl.DateTimeFormat(dateLocale, { day: '2-digit', month: '2-digit', year: 'numeric' }).format(new Date(`${item.firstAbsenceOn}T00:00:00Z`))} · {item.status === 'RECOVERY_WINDOW' ? labels.absenceRecovery : labels.absenceTitle}</span></span></span><span className="flex items-center gap-3 pl-13 text-sm sm:pl-0"><span className="tabular-nums text-muted-foreground">{item.days} {labels.absenceDays}</span><ArrowRight aria-hidden="true" className="text-accent-foreground" size={17} /></span></Link></li>)}</ul> : <p className="p-6 text-center text-sm text-muted-foreground">{labels.noActiveAbsences}</p>}</section></section>

        <aside className="space-y-4"><header><p className="eyebrow">{labels.yourPriorities}</p><h2 className="mt-1 text-2xl font-semibold tracking-tight">{labels.prioritiesBody}</h2></header><article className="overflow-hidden rounded-[1.5rem] border bg-surface shadow-[0_1rem_2.5rem_color-mix(in_srgb,var(--primary)_8%,transparent)]"><div className="border-b bg-accent/45 p-5"><div className="flex items-start justify-between gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-primary text-primary-foreground"><CalendarDays aria-hidden="true" size={20} /></span><span className="rounded-full bg-surface px-2.5 py-1 text-xs font-bold tabular-nums text-accent-foreground">{data.reminders.length}</span></div><h3 className="mt-5 text-lg font-semibold">{labels.remindersTitle}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{labels.remindersDescription}</p></div><div className="p-5">{data.reminders.length > 0 ? <ul className="divide-y divide-border/70">{data.reminders.slice(0, 4).map((reminder) => <li className="py-3 first:pt-0 last:pb-0" key={reminder.recipientId}><p className="font-semibold leading-5">{reminder.title}</p><time className="mt-1 block text-xs text-muted-foreground" dateTime={reminder.remindAt}>{formatDateTime(reminder.remindAt, { locale: dateLocale, dateFormat, timeFormat })}</time></li>)}</ul> : <div className="rounded-xl border border-dashed bg-background p-4 text-sm leading-6 text-muted-foreground"><CircleDashed aria-hidden="true" className="mb-2 text-success" size={20} />{labels.noReminders}</div>}<div className="mt-5 flex flex-wrap gap-3"><Link className="button-primary flex-1 gap-2" href="/reminders">{labels.openReminders}<ArrowRight aria-hidden="true" size={15} /></Link>{data.reminders.length > 4 ? <Link className="button-secondary" href="/reminders">{labels.moreReminders}</Link> : null}</div></div></article>

          <article className="rounded-[1.5rem] border border-dashed border-primary/25 bg-surface/75 p-5"><div className="flex items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-muted text-primary"><Wrench aria-hidden="true" size={18} /></span><div><h3 className="font-semibold">{labels.workInProgress}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{labels.workInProgressBody}</p></div></div><ul className="mt-5 grid gap-2">{futureItems.map((item) => <li className="flex items-center justify-between gap-3 rounded-xl bg-background px-3.5 py-3 text-sm" key={item}><span className="flex items-center gap-2.5"><BriefcaseBusiness aria-hidden="true" className="text-muted-foreground" size={15} />{item}</span><span className="text-xs font-medium text-muted-foreground">{labels.futureSource}</span></li>)}</ul></article></aside>
      </div>

      <section className="mt-8 flex flex-col gap-4 rounded-[1.5rem] border bg-surface/80 p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between sm:p-6"><div><p className="text-sm font-semibold">{labels.quickLinks}</p><p className="mt-1 text-sm text-muted-foreground">{labels.dashboardHint}</p></div><div className="flex flex-wrap gap-2"><Link className="button-secondary gap-2" href="/dashboard"><LayoutDashboard aria-hidden="true" size={16} />{labels.openDashboard}</Link><Link className="button-secondary gap-2" href="/hr-calendar"><CalendarDays aria-hidden="true" size={16} />{labels.calendar}</Link><Link className="button-secondary gap-2" href="/insights"><BriefcaseBusiness aria-hidden="true" size={16} />{labels.insights}</Link></div></section>
    </main></div>
}
