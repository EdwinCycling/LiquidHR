import { redirect } from 'next/navigation'
import { LeaveCatalogPage } from '@/components/leave/leave-catalog-page'
import { LeaveLedgerPanel } from '@/components/leave/leave-ledger-panel'
import { LeaveEmployeeSetsPanel } from '@/components/leave/leave-employee-sets-panel'
import { AdminSettingsPageHeader } from '@/components/settings/admin-settings-page-header'
import { AuthorizationError, requirePermission } from '@/lib/auth/permissions'
import { listLeaveCatalog } from '@/lib/leave/leave-service'
import { getTranslator } from '@/lib/i18n/server'

export default async function LeaveAccrualSettingsPage() {
  try {
    await requirePermission('leave:read')
  } catch (error) {
    if (error instanceof AuthorizationError) redirect('/geen-toegang')
    throw error
  }

  const [catalog, pageLabels, settingsLabels] = await Promise.all([listLeaveCatalog(), getTranslator('leave'), getTranslator('settings')])
  return <div className="mx-auto w-full max-w-7xl space-y-8 px-5 py-8 lg:px-10"><AdminSettingsPageHeader backLabel={settingsLabels('admin.backToOverview')} backHref="/settings" eyebrow={settingsLabels('admin.eyebrow')} title={pageLabels('page.title')} subtitle={pageLabels('page.subtitle')} /><LeaveCatalogPage initial={catalog} labels={{ addType: pageLabels('page.addType'), addWorkHour: pageLabels('page.addWorkHour'), addOvertime: pageLabels('page.addOvertime'), priorityRules: pageLabels('page.priorityRules'), showInactive: pageLabels('page.showInactive'), empty: pageLabels('page.empty'), emptyDescription: pageLabels('page.emptyDescription'), active: pageLabels('page.active'), inactive: pageLabels('page.inactive'), moreActions: pageLabels('page.moreActions'), colorOverview: pageLabels('page.colorOverview'), colorOverviewDescription: pageLabels('page.colorOverviewDescription'), usedBy: pageLabels('page.usedBy'), noColorUsage: pageLabels('page.noColorUsage'), colorUnused: pageLabels('page.colorUnused'), colorOptions: { blue: pageLabels('type.colorBlue'), teal: pageLabels('type.colorTeal'), green: pageLabels('type.colorGreen'), orange: pageLabels('type.colorOrange'), red: pageLabels('type.colorRed'), primary: pageLabels('type.colorPrimary'), success: pageLabels('type.colorSuccess'), warning: pageLabels('type.colorWarning'), destructive: pageLabels('type.colorDestructive'), accent: pageLabels('type.colorAccent'), muted: pageLabels('type.colorMuted'), sidebar: pageLabels('type.colorSidebar') }, tabs: { leave: pageLabels('page.tabs.leave'), overtime: pageLabels('page.tabs.overtime'), workHours: pageLabels('page.tabs.workHours') }, columns: { name: pageLabels('page.columns.name'), accrual: pageLabels('page.columns.accrual'), expiry: pageLabels('page.columns.expiry'), approval: pageLabels('page.columns.approval'), category: pageLabels('page.columns.category') }, approvalYes: pageLabels('page.approvalYes'), approvalNo: pageLabels('page.approvalNo'), perYear: pageLabels('page.perYear'), unlimited: pageLabels('page.unlimited'), noExpiry: pageLabels('page.noExpiry'), yearEnd: pageLabels('page.yearEnd'), monthsAfterYear: pageLabels('page.monthsAfterYear'), notConfigured: pageLabels('page.notConfigured'), contractHours: pageLabels('type.contractHours'), workedHours: pageLabels('type.workedHours'), payrollPeriod: pageLabels('type.payrollPeriod'), yearly: pageLabels('type.yearly') }} /><LeaveEmployeeSetsPanel catalog={catalog} labels={{ title: pageLabels('page.employeeSets.title'), description: pageLabels('page.employeeSets.description'), empty: pageLabels('page.employeeSets.empty'), add: pageLabels('page.employeeSets.add'), addMember: pageLabels('page.employeeSets.addMember'), name: pageLabels('page.employeeSets.name'), profile: pageLabels('page.employeeSets.profile'), priority: pageLabels('page.employeeSets.priority'), members: pageLabels('page.employeeSets.members'), active: pageLabels('page.employeeSets.active'), inactive: pageLabels('page.employeeSets.inactive'), search: pageLabels('page.employeeSets.search'), showInactive: pageLabels('page.employeeSets.showInactive'), modalTitle: pageLabels('page.employeeSets.modalTitle'), memberModalTitle: pageLabels('page.employeeSets.memberModalTitle'), employee: pageLabels('page.employeeSets.employee'), validFrom: pageLabels('page.employeeSets.validFrom'), validUntil: pageLabels('page.employeeSets.validUntil'), descriptionField: pageLabels('page.employeeSets.descriptionField'), save: pageLabels('page.employeeSets.save'), cancel: pageLabels('page.employeeSets.cancel'), saving: pageLabels('page.employeeSets.saving'), saved: pageLabels('page.employeeSets.saved'), failed: pageLabels('page.employeeSets.failed') }} /><LeaveLedgerPanel labels={{ title: pageLabels('page.ledgerTitle'), description: pageLabels('page.ledgerDescription'), yearStatus: pageLabels('page.yearStatus'), yearLocked: pageLabels('page.yearLocked'), yearActive: pageLabels('page.yearActive'), yearFuture: pageLabels('page.yearFuture'), closeYear: pageLabels('page.closeYear'), closeYearConfirm: pageLabels('page.closeYearConfirm'), closeYearDone: pageLabels('page.closeYearDone'), empty: pageLabels('page.ledgerEmpty'), failed: pageLabels('page.ledgerFailed'), loading: pageLabels('page.ledgerLoading'), working: pageLabels('page.ledgerWorking') }} /></div>
}
