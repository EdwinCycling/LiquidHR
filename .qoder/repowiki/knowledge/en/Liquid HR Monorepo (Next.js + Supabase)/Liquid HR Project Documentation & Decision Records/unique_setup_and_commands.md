No build or runtime commands — this is a pure documentation module. The `docs/README.md` defines the mandatory reading routing per change type and the branch/deployment agreement (`main` is the only persistent branch; Vercel Production builds from GitHub `main`; Supabase migrations are applied manually before main deployment). New ADR/FDR files must follow the naming pattern and include at minimum datum, status, context, besluit, gevolgen, and vervangen besluiten.