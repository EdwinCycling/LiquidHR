# Roadmap 3 — 9-grid / vlootschouw

## Handoff

- Baseline: `abfa0bbb7db628f588faa3d4818a4f4663f27b46`
- Branch: `work/r3-nine-grid`
- Worktree: `.codex-worktrees/r3-nine-grid`
- Intended runtime: `http://localhost:3110`
- Scope: `/workforce/9-grid`, direct 9-grid component/service/API/test work
- Explicitly out of scope: Workforce landing, Continuous Appraisal, Talent general, Star Performers, generic Foundation work and central delivery documents

## Implemented

- `/workforce/9-grid` now uses the existing `PageShell`/`PageHeader` foundation contract.
- Reworked the 9-grid workbench into a dense domain-specific matrix with semantic axes, real employee avatars or initials, counts, status badges and responsive grid sizing.
- Preserved native drag/drop and added a keyboard/click placement fallback per cell.
- Preserved the existing score persistence routes and read-after-write refresh behavior.
- Added clear employee context: profile link, current position, previous position and movement direction.
- Added confirmation for campaign start/close/reopen and manager team submission.
- Reused Foundation `Button`, `Badge`, `DropdownSelect`, `EmptyState`, `Surface`, `Textarea`, `TextInput`, `ConfirmDialog`, `FormField` and `SectionHeader`; no generic Foundation component was added.
- Added NL/EN keys for placement, context, movement and confirmation copy.
- Added Talent-module guards to campaign update and manual reminder services.
- Extended targeted 9-grid rule tests for empty, up, down, stable and new movement semantics.

## Contract preservation

- Campaign-based nine-cell model remains `LOW | NORMAL | HIGH` × `LOW | NORMAL | HIGH`.
- Performance/potential semantics and server-derived grid cells remain unchanged.
- Existing employee snapshot, team scope, permissions, API routes, RLS, lifecycle rules, reminder infrastructure and score mutations remain the source of truth.
- No schema, migration, remote Supabase, push, merge or deploy action was performed.

## FOUNDATION_GAP

`FOUNDATION_GAP LATER`: there is no generic Foundation primitive for a talent-specific 3×3 position matrix with employee chips, native drag/drop, keyboard placement fallback and score-cell context. The current need is domain-specific to this route and is implemented locally in `components/talent/talent-review-workspace.tsx`. If later reuse is proven, the minimum shared API should cover ordered axes/cells, scoped entity chips, read-only/editable modes, accessible placement action, drop callbacks and focus/selection state. Candidate future location: a domain-neutral pattern only after a second concrete use case; no generic addition in this slice.

## Verification

GREEN locally:

- `npm.cmd run type-check -w @liquid-hr/hr-suite`
- `npm.cmd run lint -w @liquid-hr/hr-suite` — 0 errors, 8 pre-existing warnings outside this slice
- `npm.cmd run check:i18n -w @liquid-hr/hr-suite` — 33 namespaces with equal NL/EN keys
- `npm.cmd run test --workspace @liquid-hr/hr-suite -- lib/talent-review` — 2 files, 7 tests passed
- `git diff --check`

## Acceptance status

`OPEN / BLOCKED BY ENVIRONMENT`.

The matching worktree server was started on port 3110 and the route was opened with agent-browser. Rendering stopped in the shared Supabase proxy before authentication because the worktree has no `NEXT_PUBLIC_SUPABASE_URL` or `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. The server returned the existing “project URL and Key are required” error; no HR/Manager session, real data-load, mutation, HTTP status/readback/restore cycle, negative permission/scope check, 390px browser check or LinkedHR browser check could be established. No credentials were inspected or worked around, and no test data was changed.

Required continuation when the canonical local environment and fixture sessions are available:

1. Authenticate as actual HR and Manager personas.
2. Prove tenant/team scope and negative employee/permission access.
3. Load a real campaign/team and capture the matrix and detail context at desktop and 390px.
4. Snapshot original position, move one employee, record mutation HTTP status, refresh/read back, then restore and verify the original position.
5. Check Default and LinkedHR themes and console/network errors.

Runtime and browser session were stopped after the blocked acceptance attempt.

## Acceptance retry — 2026-08-22

The required retry preflight could not start because the explicitly specified canonical source file was absent:

- Source `C:\Users\Edwin\Documents\Apps\LiquidHR\apps\hr-suite\.env.local`: `Test-Path` false; `Copy-Item -Force` failed because the source path does not exist.
- Target `C:\Users\Edwin\Documents\Apps\LiquidHR\.codex-worktrees\r3-nine-grid\apps\hr-suite\.env.local`: `Test-Path` false; no substitute env file was copied and no env contents or secrets were displayed.
- `npm.cmd run fixtures:talent-auth -w @liquid-hr/hr-suite`: blocked before authentication, exit code 9 (`node: .env.local: not found`).

Because the mandatory TEST environment precondition was unavailable, this retry did not start a new server and did not claim any browser/API/persona/responsive/theme acceptance. No fixture password was changed, no remote schema/database action was performed, no test data was changed, and the existing runtime remained stopped. The acceptance blocker is environment availability, not a newly diagnosed in-scope product bug.

## Acceptance retry — canonical TEST-env restored

The canonical source was copied with `Copy-Item -Force` from `apps/hr-suite/.env.local` in the main workspace. The worktree target was verified with `Test-Path=True`; env contents were not displayed. `npm.cmd run fixtures:talent-auth -w @liquid-hr/hr-suite` completed and updated only `hr-admin`, `manager` and `employee` fixtures.

Real browser/API evidence on the matching worktree server `http://localhost:3110`:

- `POST /login` returned `303` for TEST HR, TEST MANAGER and TEST EMPLOYEE; the three sessions reached `/dashboard/start`.
- TEST HR: `/workforce/9-grid` returned `200` at 1440x900 and 390x844. Default (`liquid-navy`) and LinkedHR (`linkedhr`) both rendered; `scrollWidth` equalled the viewport width at 390px; the 9-grid copy and matrix headings were present. `GET /api/talent/review/campaigns` returned `200`.
- The tenant had no campaign data, so a recognizable temporary campaign `TEST acceptance 9-grid 2026-08-22` was created through the real API: `POST /api/talent/review/campaigns` returned `201`.
- Starting that campaign returned `400` with `TALENT_REVIEW_ASSIGNMENTS`; the campaign remained `DRAFT`, no manager assignments or scores were created, and `GET /api/talent/review/campaigns/{campaignId}/workspace?mode=manager` returned `404` with `TALENT_REVIEW_CAMPAIGN_NOT_FOUND`.
- TEST MANAGER: `/workforce/9-grid` returned `200` at desktop and 390x844 in Default and LinkedHR. The empty manager workspace rendered without horizontal overflow, but no manager campaign/team was available, so no score mutation could be attempted.
- TEST EMPLOYEE: `/workforce/9-grid` redirected to `/geen-toegang` (route responses `200`); `GET /api/talent/review/campaigns` returned `403`. This is the expected authorization-negative.
- Cleanup close attempt `POST /api/talent/review/campaigns/{campaignId}/close` returned `400` with `TALENT_REVIEW_CAMPAIGN_CLOSE_INVALID` because start failed. The product exposes no delete/archive operation for a DRAFT campaign, so the recognizable DRAFT fixture remains as a documented cleanup blocker; no direct database bypass was used.
- No relevant uncaught browser JavaScript errors were observed. Console entries corresponded to the real blocked HTTP responses above (HR start `400`, manager workspace `404`, employee negative `403`).

Mutation/readback/restore is `BLOCKED` before the score endpoint because the real start RPC cannot create assignments in this TEST tenant. No score or other test data was changed beyond the temporary campaign creation described above. No generic Foundation, schema, remote migration, push, merge or deploy action was performed. The local dev server was stopped after the retry.
